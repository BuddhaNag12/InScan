import React from 'react';
import {Text} from 'react-native';

export const SignInTitle = () => {
  return <Text style={{fontFamily: 'Roboto', color: '#fff'}}>Sign In</Text>;
};
export const SignUpTitle = () => {
  return <Text style={{fontFamily: 'Roboto', color: '#fff'}}>Sign Up</Text>;
};
